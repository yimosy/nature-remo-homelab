# このファイルを .env にコピーして値を設定してください
#   cp .env.example .env
# .env は git 管理外です(コミットされません)

# 管理者ログイン用トークン(必須)。生成例: openssl rand -base64 24
ADMIN_TOKEN=

# 許可するネットワーク(任意、カンマ区切り CIDR)
# ALLOW_CIDRS=192.168.1.0/24

# リバースプロキシ配下で X-Forwarded-For を信頼する場合のみ true
# TRUST_PROXY=true
